import { WebOrDesktopDirective } from './web-or-desktop.directive';

describe('WebOrDesktopDirective', () => {
  it('should create an instance', () => {
    const directive = new WebOrDesktopDirective(<any>{});
    expect(directive).toBeTruthy();
  });
});
